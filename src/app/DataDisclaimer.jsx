export default function DataDisclaimer() {
  return (
    <p className="text-xs text-zinc-400 max-w-md text-center px-4">
      Based on public GitHub activity only — private repos aren't visible.
      "Commits" reflects work you personally authored, not entire repos or others' contributions.
    </p>
  );
}